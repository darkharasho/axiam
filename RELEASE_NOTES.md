# Release Notes

Version v1.1.7 — April 22, 2026

## Fixes

The Linux AppImage is now named `axiam.AppImage`. This fixes compatibility with Gear Lever and other AppImage managers that expect the standard `.AppImage` extension. Also fixes the Windows installer which was accidentally broken in v1.1.6.
